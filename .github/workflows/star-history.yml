name: Refresh Star History

on:
  schedule:
    - cron: '*/15 * * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: star-history-${{ github.repository }}
  cancel-in-progress: true

jobs:
  refresh:
    name: Refresh chart data
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    steps:
      - name: Send authenticated repository snapshot
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          INGEST_TOKEN: ${{ secrets.STAR_HISTORY_INGEST_TOKEN }}
          REPOSITORY: ${{ github.repository }}
          STAR_HISTORY_ENDPOINT: ${{ vars.STAR_HISTORY_ENDPOINT }}
        run: |
          set -euo pipefail
          test -n "${INGEST_TOKEN}"
          test -n "${STAR_HISTORY_ENDPOINT}"
          metadata_file="${RUNNER_TEMP}/star-history-metadata.json"
          stargazers_file="${RUNNER_TEMP}/star-history-stargazers.json"
          payload_file="${RUNNER_TEMP}/star-history-payload.json"

          gh api \
            --header "Accept: application/vnd.github+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "repos/${REPOSITORY}" > "${metadata_file}"
          gh api \
            --paginate \
            --slurp \
            --header "Accept: application/vnd.github.star+json" \
            --header "X-GitHub-Api-Version: 2022-11-28" \
            "repos/${REPOSITORY}/stargazers?per_page=100" > "${stargazers_file}"

          jq -n \
            --arg repository "${REPOSITORY}" \
            --slurpfile metadata "${metadata_file}" \
            --slurpfile pages "${stargazers_file}" \
            '($pages[0] | add // [] | map(select(.starred_at != null and .user.id != null)) | sort_by(.user.id, .starred_at) | group_by(.user.id) | map(min_by(.starred_at))) as $stars
            | {
                repository: $repository,
                createdAt: $metadata[0].created_at,
                checkedAt: (now | todateiso8601),
                currentStars: $metadata[0].stargazers_count,
                stargazers: [$stars[] | {user: {id: .user.id}, starred_at: .starred_at}]
              }' > "${payload_file}"

          curl \
            --fail-with-body \
            --retry 3 \
            --retry-all-errors \
            --connect-timeout 15 \
            --max-time 240 \
            --request POST \
            --header "Authorization: Bearer ${INGEST_TOKEN}" \
            --header "Content-Type: application/json" \
            --data-binary "@${payload_file}" \
            "${STAR_HISTORY_ENDPOINT}"
